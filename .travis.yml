language: python

before_install:
 - sudo apt-get update
 - sudo apt-get install python-opencv
 - sudo dpkg -L python-opencv
install:
  - "pip install -r requirements.txt"

python:
  - "2.7"
script:
  - python run-tests.py
